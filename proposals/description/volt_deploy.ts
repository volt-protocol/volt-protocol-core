import { ProposalDescription } from '@custom-types/types';

const volt_deploy: ProposalDescription = {
  title: 'FIP-Deploy: Create Volt',
  commands: [],
  description: ``
};

export default volt_deploy;
